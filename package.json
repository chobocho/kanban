{
  "name": "kanban",
  "version": "1.0.0",
  "description": "HTML5 Kanban Board (Trello clone) - no runtime dependencies",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "tsc && node js/test/run.js",
    "smoke": "tsc && node test/smoke.mjs",
    "serve": "python -m http.server 8001"
  },
  "license": "MIT",
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
