# Normalize text files to LF in the repo.
* text=auto eol=lf

# Keep the Windows build script untouched: it is CP949-encoded with CRLF line
# endings so Korean text renders correctly in the cmd console. The binary rule
# comes last so it wins, stopping git from re-encoding or converting newlines.
build.bat binary
